alert("Your message here")
// A built-in JavaScript function used to display a popup message box to the user.
// It pauses the program until the user clicks “OK”.

confirm("Are you sure?");
// A built-in JavaScript function that shows a popup with "OK" and "Cancel" buttons.
// It asks the user for confirmation (yes/no, true/false)